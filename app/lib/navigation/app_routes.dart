/// Named routes for screens pushed from chrome (the nav bar).
/// Keep this file free of screen imports to avoid chrome ↔ page cycles.
abstract final class AppRoutes {
  static const changePassword = '/change-password';
}
